# run-kotlin-native

## How to build

```shell script
./gradlew clean build
```

### Compile binaries for specific platform

for MacOS,

```shell script
./gradlew clean macosX64Binaries
```

for Linux (amd64),

```shell script
./gradlew clean linuxX64Binaries
```
